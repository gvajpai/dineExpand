% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expand_dining.R
\name{expand_dining}
\alias{expand_dining}
\title{Expand a seed dictionary using restaurant-domain word embeddings}
\usage{
expand_dining(
  seed,
  n = 20L,
  threshold = 0.65,
  seed_mode = c("individual", "centroid"),
  interactive = base::interactive(),
  vectors = NULL,
  cache_dir = rappdirs::user_data_dir("dineExpand")
)
}
\arguments{
\item{seed}{Character vector of seed words representing the construct to
expand. Examples:
\itemize{
\item Revisit intention: \code{c("revisit", "return", "loyalty")}
\item Food neophobia: \code{c("adventurous", "novel", "unfamiliar", "exotic")}
\item Authenticity: \code{c("authentic", "traditional", "genuine", "local")}
\item Service quality: \code{c("attentive", "courteous", "prompt", "helpful")}
}}

\item{n}{Integer. Number of candidates to surface per seed word. Default
\code{20L}.}

\item{threshold}{Numeric in [0, 1]. Minimum cosine similarity required.
Default \code{0.65}. Values above 0.80 return near-synonyms only; 0.60–0.70
gives a broader semantic neighbourhood.}

\item{seed_mode}{Character. Multi-seed strategy:
\itemize{
\item \code{"individual"} \emph{(default)} — finds neighbours per seed, deduplicates.
The \verb{$seed} column shows which seed drove each match.
\item \code{"centroid"} — finds words nearest the semantic centre of all seeds.
}}

\item{interactive}{Logical. If \code{TRUE} (default in an R session), launches
the word-by-word review wizard. If \code{FALSE}, returns the ranked data frame
directly — use this in scripts and RMarkdown.}

\item{vectors}{Numeric matrix or \code{NULL}. Pre-loaded vector matrix from
\code{\link[=load_dining_vectors]{load_dining_vectors()}}. If \code{NULL}, vectors are loaded automatically.
Pass a pre-loaded matrix when calling this function repeatedly to avoid
reloading on each call.}

\item{cache_dir}{Character. Vector cache directory. Default:
\code{rappdirs::user_data_dir("dineExpand")}.}
}
\value{
\itemize{
\item When \code{interactive = FALSE}: a \code{data.frame} with columns \code{word},
\code{similarity}, \code{seed}, and \code{pct_match}, sorted by descending similarity.
\item When \code{interactive = TRUE}: a \code{lexiexpand_result} object (invisibly)
with \verb{$accepted}, \verb{$seed}, and \verb{$candidates}. Pass to
\code{\link[lexiExpand:export_dict]{lexiExpand::export_dict()}} to export.
}
}
\description{
Finds semantically similar words for any dining or hospitality research
construct using vectors pre-trained on Yelp restaurant reviews. A
convenience wrapper around \code{\link[lexiExpand:expand_dict]{lexiExpand::expand_dict()}} that automatically
loads restaurant vectors and applies dining-appropriate defaults.
}
\examples{
\dontrun{
vecs <- load_dining_vectors()

# Single word
expand_dining("aroma", vectors = vecs, interactive = FALSE)

# Revisit intention construct
expand_dining(c("revisit", "return", "loyalty"),
              vectors = vecs, interactive = FALSE)

# Food neophobia — centroid mode
expand_dining(c("adventurous", "novel", "unfamiliar", "exotic"),
              seed_mode = "centroid",
              vectors   = vecs,
              interactive = FALSE)

# Interactive review wizard
result <- expand_dining(c("authentic", "traditional", "genuine"),
                        vectors = vecs)
lexiExpand::export_dict(result, name = "authenticity", format = "list")
}

}
\seealso{
\code{\link[=load_dining_vectors]{load_dining_vectors()}}, \code{\link[=expand_dining_lexicon]{expand_dining_lexicon()}},
\code{\link[lexiExpand:export_dict]{lexiExpand::export_dict()}}
}
