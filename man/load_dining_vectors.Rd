% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_dining_vectors.R
\name{load_dining_vectors}
\alias{load_dining_vectors}
\title{Load pre-trained restaurant word vectors}
\usage{
load_dining_vectors(
  cache_dir = rappdirs::user_data_dir("dineExpand"),
  overwrite = FALSE,
  timeout = 300L
)
}
\arguments{
\item{cache_dir}{Character. Directory for caching the vectors file.
Defaults to a user-level data directory via \code{\link[rappdirs:user_data_dir]{rappdirs::user_data_dir()}}.}

\item{overwrite}{Logical. Re-download even if already cached. Default
\code{FALSE}.}

\item{timeout}{Integer. Download timeout in seconds. Default \code{300}.}
}
\value{
An L2-normalised numeric matrix (words × 100 dimensions) with
restaurant-domain vocabulary as row names. Compatible with
\code{\link[=expand_dining]{expand_dining()}}, \code{\link[=expand_dining_lexicon]{expand_dining_lexicon()}}, and all
\code{lexiExpand::expand_dict()} calls via the \code{vectors} argument.
}
\description{
Downloads and caches word vectors pre-trained on 512,140 Yelp restaurant
reviews (100 dimensions, ~100,000 vocabulary terms). The download
(~33 MB) happens \strong{once}; subsequent calls load from the local cache or
from the session cache (instant).
}
\details{
The vectors were trained using the GloVe algorithm (Pennington et al.,
2014) on restaurant reviews from the Yelp Open Dataset. Training used
a co-occurrence window of 5, 100 dimensions, and 20 iterations.
Restaurant-specific vocabulary (e.g. \emph{umami}, \emph{gastropub},
\emph{waitstaff}) clusters meaningfully in this space, making it
far more suitable for hospitality research than general-purpose
embeddings trained on Wikipedia or news corpora.

The vectors file is hosted as a GitHub release asset at:
\verb{https://github.com/gvajpai/dineExpand/releases/download/v0.1.0-data/yelp_restaurant_glove_100d.rds}
}
\examples{
\dontrun{
# Download and cache (first run only)
vecs <- load_dining_vectors()

# Force re-download
vecs <- load_dining_vectors(overwrite = TRUE)

# Custom cache location
vecs <- load_dining_vectors(cache_dir = "~/my_vectors/")
}

}
\seealso{
\code{\link[=expand_dining]{expand_dining()}}, \code{\link[=expand_dining_lexicon]{expand_dining_lexicon()}},
\code{\link[=train_dining_vectors]{train_dining_vectors()}}
}
