% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train_dining_vectors.R
\name{train_dining_vectors}
\alias{train_dining_vectors}
\title{Train custom restaurant word vectors from a text corpus}
\usage{
train_dining_vectors(
  texts,
  dims = 100L,
  vocab_size = 100000L,
  min_count = 5L,
  window = 5L,
  iterations = 20L,
  n_threads = parallel::detectCores(),
  cache_path = NULL
)
}
\arguments{
\item{texts}{Character vector of restaurant reviews or any hospitality text.}

\item{dims}{Integer. Embedding dimensions. Default \code{100L}. Higher values
capture more nuance but require more memory and training time.}

\item{vocab_size}{Integer. Maximum vocabulary size. Default \code{100000L}.}

\item{min_count}{Integer. Minimum token frequency to include in vocabulary.
Default \code{5L}. Raise to \code{10} on limited memory.}

\item{window}{Integer. Co-occurrence window size. Default \code{5L}.}

\item{iterations}{Integer. Training epochs. Default \code{20L}. Increase to
\code{30} for higher quality on large corpora.}

\item{n_threads}{Integer. CPU threads for training. Defaults to all
available cores via \code{\link[parallel:detectCores]{parallel::detectCores()}}.}

\item{cache_path}{Character or \code{NULL}. If provided, saves the trained
matrix as an RDS file at this path. Default \code{NULL}.}
}
\value{
An L2-normalised numeric matrix (words × dimensions), invisibly.
Compatible with \code{\link[=expand_dining]{expand_dining()}}, \code{\link[=expand_dining_lexicon]{expand_dining_lexicon()}}, and all
\code{lexiExpand} functions via the \code{vectors} argument.
}
\description{
Trains GloVe word embeddings on a character vector of restaurant reviews
and returns an L2-normalised matrix compatible with \code{\link[=expand_dining]{expand_dining()}} and
\code{\link[=expand_dining_lexicon]{expand_dining_lexicon()}}. Useful when you have your own proprietary review
corpus or want domain-specific vectors beyond the default Yelp model.
}
\details{
Requires the \pkg{text2vec} package.
}
\examples{
\dontrun{
library(data.table)
reviews <- fread("my_restaurant_reviews.csv")

vecs <- train_dining_vectors(
  texts      = reviews$text,
  dims       = 100L,
  cache_path = "~/my_restaurant_vectors.rds"
)

expand_dining(c("aroma", "flavor"), vectors = vecs)
}

}
\seealso{
\code{\link[=load_dining_vectors]{load_dining_vectors()}}, \code{\link[=expand_dining]{expand_dining()}}
}
