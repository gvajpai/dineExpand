% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expand_dining_lexicon.R
\name{expand_dining_lexicon}
\alias{expand_dining_lexicon}
\title{Expand a full lexicon data frame using restaurant-domain embeddings}
\usage{
expand_dining_lexicon(
  lexicon,
  word_col = NULL,
  dim_col = NULL,
  n = 20L,
  threshold = 0.65,
  seed_mode = c("individual", "centroid"),
  vectors = NULL,
  cache_dir = rappdirs::user_data_dir("dineExpand")
)
}
\arguments{
\item{lexicon}{A data frame with at least two columns: words and dimension
labels. Column names are detected automatically (first = words, second =
dimensions) or specified via \code{word_col} and \code{dim_col}.}

\item{word_col}{Character. Name of the word column. If \code{NULL} (default),
the first column is used.}

\item{dim_col}{Character. Name of the dimension column. If \code{NULL}
(default), the second column is used.}

\item{n}{Integer. Candidates per seed word. Default \code{20L}.}

\item{threshold}{Numeric. Minimum cosine similarity. Default \code{0.65}.}

\item{seed_mode}{Character. \code{"individual"} (default) or \code{"centroid"}.
See \code{\link[=expand_dining]{expand_dining()}}.}

\item{vectors}{Numeric matrix or \code{NULL}. Pre-loaded dining vectors. If
\code{NULL}, loaded automatically via \code{\link[=load_dining_vectors]{load_dining_vectors()}}. Pass a
pre-loaded matrix when expanding multiple lexicons in one session.}

\item{cache_dir}{Character. Vector cache directory.}
}
\value{
A \code{data.frame} with columns \code{word}, \code{similarity}, \code{seed},
\code{pct_match}, and \code{dimension} — one row per candidate across all
dimensions, sorted by dimension then descending similarity. Words
already in the lexicon are excluded.
}
\description{
Accepts a two-column data frame (words + dimension labels) and expands
every dimension using vectors pre-trained on Yelp restaurant reviews.
A convenience wrapper around \code{\link[lexiExpand:expand_lexicon]{lexiExpand::expand_lexicon()}} with automatic
dining vector loading.
}
\examples{
\dontrun{
vecs <- load_dining_vectors()

# Any two-column lexicon
my_dict <- data.frame(
  word      = c("aroma",     "flavor",   "texture",
                "delighted", "excited",  "moved",
                "revisit",   "return",   "loyal"),
  dimension = c("sensory",   "sensory",  "sensory",
                "affect",    "affect",   "affect",
                "revisit",   "revisit",  "revisit")
)

candidates <- expand_dining_lexicon(my_dict, vectors = vecs)
table(candidates$dimension)

# Custom column names
expand_dining_lexicon(my_dict,
                      word_col = "term",
                      dim_col  = "construct",
                      vectors  = vecs)
}

}
\seealso{
\code{\link[=expand_dining]{expand_dining()}}, \code{\link[=load_dining_vectors]{load_dining_vectors()}},
\code{\link[lexiExpand:export_dict]{lexiExpand::export_dict()}}
}
